---
title: "components/certificate_transparency: Migrate to new crypto/hash APIs"
date: 2026-09-10
author: ch123song
contribution_url: https://crrev.com/c/8382616
module: components/certificate_transparency
kind: refactor
status: merged
---

components/certificate_transparency  디렉토리 내 crypto/sha2.h 를 crypto/hash.h 로 마이그레이션했습니다.


## 문제 설명

- components/certificate_transparency  디렉토리 내 crypto/sha2.h 를 crypto/hash.h 로 교체

## 해결 내용

1. components/certificate_transparency/chrome_ct_policy_enforcer.cc 수정
 ```diff
 - #include "crypto/sha2.h"
+ #include "crypto/hash.h"
  ```

 ```diff
std::optionalbase::Time ChromeCTPolicyEnforcer::GetLogDisqualificationTime(
std::string_view log_id) const {
- CHECK_EQ(log_id.size(), crypto::kSHA256Length);
+ CHECK_EQ(log_id.size(), crypto::hash::kSha256Size);
 ```

2. components/certificate_transparency/chrome_require_ct_delegate.cc 수정
 ```diff
 - #include "crypto/sha2.h"
  ```

3. components/certificate_transparency/ct_known_logs.cc 수정
```diff
- #include "crypto/sha2.h"
+ #include "crypto/hash.h"
  ```

```diff
std::optionalbase::Time ChromeCTPolicyEnforcer::GetLogDisqualificationTime(
std::string_view log_id) const {
- CHECK_EQ(log_id.size(), crypto::kSHA256Length);
+ CHECK_EQ(log_id.size(), crypto::hash::kSha256Size);
 ```

4. components/certificate_transparency/ct_known_logs_unittest.cc 수정
```diff
- #include "crypto/sha2.h"
  ```

## 테스트 방법

- `autoninja -C out/Default chrome` 통과
- `autoninja -C out/Default components_unittests` 
   `./out/Default/components_unittests --gtest_filter='CTKnownLogsTest.*'` 통과

## 배운 점

- unittests 를 해볼 수 있었습니다.

## 참고 자료

- [issue url](https://issues.chromium.org/issues/372283556)

